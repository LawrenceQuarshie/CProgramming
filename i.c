#include <stdio.h>

int main() {
    // good variable name
    int minutesperhour = 60;

    // okay, but not so easy to understand what m is actually is
    int m = 60;

    printf("%d\n", minutesperhour);
    printf("%d\n", m);
    return 0;
}